# Public API base URL (leave empty to use CDN manifest/static fallback only)
VITE_API_URL=http://localhost:3001
# Base URL for images (the CDN in front of the bucket)
VITE_CDN_URL=https://ik.imagekit.io/ilalex
